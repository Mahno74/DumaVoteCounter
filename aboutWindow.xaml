<Window x:Class="DumaVoteCounter.aboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DumaVoteCounter"
        mc:Ignorable="d"
       TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="12"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignBackground}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="aboutWindow" Height="150" Width="200" ResizeMode="CanResize" WindowStyle="None" MouseLeftButtonDown="Dragging" WindowStartupLocation="CenterScreen" Topmost="True">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <Label Content="Программа для вывода" HorizontalAlignment="Center"/>
        <Label Content="результатов голосования" HorizontalAlignment="Center"/>
        <Label Content="Юрасов В.В. © 2021" HorizontalAlignment="Center"/>
        <Label Content="2665603@gmail.com" HorizontalAlignment="Center"/>
        <Button Margin="0 8 0 0"  Style="{StaticResource MaterialDesignRaisedAccentButton}"  Content="OK" Width="65" Height="25" Click="Close"/>
    </StackPanel>
</Window>
